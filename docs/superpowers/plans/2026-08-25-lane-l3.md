# 泳道 L3 实施计划（零改动染色 + TTL 回收 + 泳道可视化）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 应用部署到 feature 泳道即自动参与联调（零代码改动），闲置泳道自动回收，泳道部署态/运行态可视化。

**Architecture:** SDK 加 `SelfLane()`（读 `PAAS_LANE_ID` env）作 lane 解析的默认回落；core 内周期 laneGC 任务按闲置 TTL 删非 default 泳道 Workload（经 Applier 投影删 CRD）；前端泳道矩阵 + trace `paas.lane` 着色。

**Tech Stack:** Go（sdk/paas-registry 独立 module）、Vue 3 + Element Plus、既有 Jaeger trace / pipeline runs。

**Spec:** `docs/superpowers/specs/2026-08-25-lane-l3-design.md`

## Global Constraints

- `LaneDefault = "default"`（基线）；SDK header 名 `x-paas-lane`；trace 属性名 `paas.lane`。
- laneGC：默认 TTL 72h（env `PAAS_LANE_TTL`）、周期 30min（env `PAAS_LANE_GC_INTERVAL`，0=禁用）、单轮删除上限 20、prod 环境泳道不回收。
- 闲置判定：`max(Workload.CreatedAt, 该 (app,lane) 最近 pipeline run FinishedAt)`（Workload 无 UpdatedAt，避免 migration）。
- 依赖倒置：workload 包不得 import pipeline 包（接口注入）。
- 注释语言与代码库一致（中文）。

---

### Task 1: SDK SelfLane 默认染色

**Files:**
- Modify: `sdk/paas-registry/lane.go`
- Modify: `sdk/paas-registry/registry.go:80-86`（GetService lane 解析）
- Test: `sdk/paas-registry/lane_test.go`

**Interfaces:**
- Produces: `SelfLane() string`（进程级缓存，读 `PAAS_LANE_ID`，空/`default` 返 `""`）；`resolveLane(ctx) string`（包内：ctx lane > SelfLane > ""）。

- [ ] **Step 1: 写失败测试**（追加到 `lane_test.go`）

```go
func TestSelfLaneFromEnv(t *testing.T) {
	t.Setenv("PAAS_LANE_ID", "feature-x")
	resetSelfLaneForTest(t)
	if got := SelfLane(); got != "feature-x" {
		t.Fatalf("SelfLane = %q, want feature-x", got)
	}
}

func TestSelfLaneDefaultEmpty(t *testing.T) {
	t.Setenv("PAAS_LANE_ID", "default")
	resetSelfLaneForTest(t)
	if got := SelfLane(); got != "" {
		t.Fatalf("default 应返空串, got %q", got)
	}
}

func TestResolveLanePriority(t *testing.T) {
	t.Setenv("PAAS_LANE_ID", "feature-env")
	resetSelfLaneForTest(t)
	// ctx 显式 > env
	ctx := WithLane(context.Background(), "feature-ctx")
	if got := resolveLane(ctx); got != "feature-ctx" {
		t.Fatalf("ctx 优先: got %q", got)
	}
	// ctx 无 lane -> env 回落
	if got := resolveLane(context.Background()); got != "feature-env" {
		t.Fatalf("env 回落: got %q", got)
	}
}

func TestApplyLaneHeaderEnvFallback(t *testing.T) {
	t.Setenv("PAAS_LANE_ID", "feature-env")
	resetSelfLaneForTest(t)
	req, _ := http.NewRequest("GET", "http://x", nil)
	ApplyLaneHeader(context.Background(), req)
	if got := req.Header.Get(LaneHeader); got != "feature-env" {
		t.Fatalf("env 回落注入: got %q", got)
	}
}
```

同文件加测试 helper：

```go
// resetSelfLaneForTest 重置 SelfLane 缓存（t.Setenv 后重新读 env）。
func resetSelfLaneForTest(t *testing.T) {
	selfLane.Store("")
	t.Cleanup(func() { selfLane.Store("") })
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sdk/paas-registry && go test ./... -run "SelfLane|ResolveLane|ApplyLaneHeader" -v`
Expected: FAIL（`SelfLane`/`resolveLane`/`selfLane` 未定义）

- [ ] **Step 3: 实现**（`lane.go` 追加；`ApplyLaneHeader` 改用 resolveLane）

```go
// selfLane 进程级缓存自身部署泳道（sync.OnceValue 语义，手动 atomic 兜空重读）。
var selfLane atomic.Value // string

// SelfLane 返回自身部署泳道（reconciler 注入的 PAAS_LANE_ID env）。
// 空缺 / "default" 返 ""（基线）。首次调用读取并缓存。
// 这是「应用零改动染色」核心：应用不挂 LaneMiddleware 时，出站调用回落到自身泳道。
func SelfLane() string {
	if v, ok := selfLane.Load().(string); ok && v != "" {
		return v
	}
	v := os.Getenv("PAAS_LANE_ID")
	if v == "" || v == "default" {
		v = "" // 基线统一空串
	}
	selfLane.Store(v)
	return v
}

// resolveLane lane 解析优先级：显式 ctx lane > SelfLane env > ""。
// ctx lane 是入口/上游指定的染色（如网关注入 header）；env 是自身部署泳道。
func resolveLane(ctx context.Context) string {
	if lane := LaneFromCtx(ctx); lane != "" {
		return lane
	}
	return SelfLane()
}
```

`ApplyLaneHeader` 尾部改为：

```go
func ApplyLaneHeader(ctx context.Context, req *http.Request) {
	lane := resolveLane(ctx)
	if lane == "" {
		return
	}
	req.Header.Set(LaneHeader, lane)
}
```

`registry.go` GetService 改：

```go
	if lane := resolveLane(ctx); lane != "" {
		u += "&lane=" + url.QueryEscape(lane)
	}
```

注意：缓存空串会导致 env 后设失效（测试场景）——`SelfLane` 里空串不缓存（`ok && v != ""` 才短路），生产 env 启动即定无影响。import 加 `"os"` + `"sync/atomic"`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sdk/paas-registry && go test ./... -v`
Expected: 全 PASS（含存量 lane_test 用例不回归）

- [ ] **Step 5: Commit**

```bash
git add sdk/paas-registry/
git commit -m "feat(sdk): SelfLane env 默认染色——应用零改动参与泳道联调"
```

---

### Task 2: laneGC 闲置回收（internal/workload）

**Files:**
- Create: `internal/workload/lanegc.go`
- Test: `internal/workload/lanegc_test.go`

**Interfaces:**
- Consumes: `workload.Repository`（既有 `List(ctx, envID, appID, laneID, wtype, service string)` / `Delete(ctx, id)`）、`Applier`（既有，Delete 已投影 CRD）。
- Produces:

```go
// LaneActivityChecker 查询某 (app, lane) 的最近活跃时间（最近终态 run 的 FinishedAt；无 run 返零值）。
// 依赖倒置：workload 不 import pipeline。查询错误时调用方跳过该 lane（fail-open 下轮再试）。
type LaneActivityChecker interface {
	LastActive(ctx context.Context, tenantID, appID, lane string) (time.Time, error)
}

// EnvType 查询环境类型（prod 跳过回收）。依赖倒置：复用 environment.EnvTypeResolver 语义。
type EnvType func(ctx context.Context, envID string) (string, error)

type LaneGC struct {
	Repos   Repository       // 跨租户列出（ListAll）+ 删除（经 Applier 装饰）
	Runs    LaneActivityChecker
	EnvType EnvType
	TTL     time.Duration    // 闲置阈值（默认 72h）
	MaxSweep int             // 单轮删除上限（默认 20）
	Now     func() time.Time // 测试注入时钟
	Log     *log.Logger      // 可空（nil 用 log.Default）
}

func (g *LaneGC) Sweep(ctx context.Context) int // 返回本轮删除数
```

- [ ] **Step 1: 写失败测试**

```go
package workload

import (
	"context"
	"testing"
	"time"
)

type gcFakeRepo struct {
	items []Workload
	deleted []string
}
func (f *gcFakeRepo) ListAll(ctx context.Context) ([]Workload, error) { return f.items, nil }
func (f *gcFakeRepo) Delete(ctx context.Context, id string) error {
	f.deleted = append(f.deleted, id)
	return nil
}
// 其余 Repository 方法嵌入 stub（同 handler_test.go 的 stubRepo 模式，Embed 补齐即可编译）。

type gcFakeRuns struct{ last map[string]time.Time }
func (f gcFakeRuns) LastActive(ctx context.Context, tenantID, appID, lane string) (time.Time, error) {
	return f.last[appID+"/"+lane], nil
}

func newGC(items []Workload, last map[string]time.Time) (*LaneGC, *gcFakeRepo) {
	repo := &gcFakeRepo{items: items}
	g := &LaneGC{
		Repos: repo, Runs: gcFakeRuns{last: last},
		EnvType: func(context.Context, string) (string, error) { return "test", nil },
		TTL: 72 * time.Hour, MaxSweep: 20,
		Now: func() time.Time { return time.Date(2026, 8, 25, 0, 0, 0, 0, time.UTC) },
	}
	return g, repo
}

func wl(id, lane string, createdAt time.Time) Workload {
	return Workload{ID: id, TenantID: "t1", AppID: "a1", EnvID: "e1", LaneID: lane, CreatedAt: createdAt}
}

var old = time.Date(2026, 8, 20, 0, 0, 0, 0, time.UTC) // 5 天前 > 72h
var recent = time.Date(2026, 8, 24, 0, 0, 0, 0, time.UTC) // 1 天前 < 72h

func TestSweepDeletesIdleLane(t *testing.T) {
	g, repo := newGC([]Workload{wl("w1", "feature-x", old)}, nil)
	if n := g.Sweep(context.Background()); n != 1 || len(repo.deleted) != 1 {
		t.Fatalf("应删 1 个, got n=%d deleted=%v", n, repo.deleted)
	}
}

func TestSweepKeepsDefaultAndFresh(t *testing.T) {
	g, repo := newGC([]Workload{wl("w1", "default", old), wl("w2", "feature-x", recent)}, nil)
	if n := g.Sweep(context.Background()); n != 0 || len(repo.deleted) != 0 {
		t.Fatalf("default/新近不应删, got %v", repo.deleted)
	}
}

func TestSweepRecentRunBlocks(t *testing.T) {
	// Workload 很旧但最近有活跃 run（联调中）-> 不回收
	g, repo := newGC([]Workload{wl("w1", "feature-x", old)},
		map[string]time.Time{"a1/feature-x": recent})
	if n := g.Sweep(context.Background()); n != 0 || len(repo.deleted) != 0 {
		t.Fatalf("活跃 run 应阻止回收, got %v", repo.deleted)
	}
}

func TestSweepSkipsProd(t *testing.T) {
	g, repo := newGC([]Workload{wl("w1", "feature-x", old)}, nil)
	g.EnvType = func(context.Context, string) (string, error) { return "prod", nil }
	if n := g.Sweep(context.Background()); n != 0 {
		t.Fatalf("prod 泳道不回收, got %d", n)
	}
}

func TestSweepMaxCap(t *testing.T) {
	items := make([]Workload, 25)
	for i := range items {
		items[i] = wl(string(rune('a'+i)), "feature-x", old)
	}
	g, repo := newGC(items, nil)
	if n := g.Sweep(context.Background()); n != 20 || len(repo.deleted) != 20 {
		t.Fatalf("单轮上限 20, got %d", n)
	}
}

func TestSweepRunCheckErrorSkips(t *testing.T) {
	g, repo := newGC([]Workload{wl("w1", "feature-x", old)}, nil)
	g.Runs = errRuns{}
	if n := g.Sweep(context.Background()); n != 0 {
		t.Fatalf("查询失败应跳过（fail-open）, got %d", n)
	}
}

type errRuns struct{}
func (errRuns) LastActive(context.Context, string, string, string) (time.Time, error) {
	return time.Time{}, context.DeadlineExceeded
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/workload/ -run TestSweep -v`
Expected: FAIL（`LaneGC` 未定义）

- [ ] **Step 3: 实现 `lanegc.go`**

```go
package workload

import (
	"context"
	"log"
	"time"
)

// LaneGC 闲置泳道回收：周期扫描非 default 泳道 Workload，闲置超 TTL 且无活跃 run 则删除。
// 「闲置」= max(Workload.CreatedAt, 最近终态 run FinishedAt) 距今超 TTL（Workload 无 UpdatedAt，
// 以创建时间 + run 活跃度近似，CI 重跑会刷新 run 侧时间，足够判定）。
type LaneGC struct {
	Repos    Repository
	Runs     LaneActivityChecker
	EnvType  EnvType
	TTL      time.Duration
	MaxSweep int
	Now      func() time.Time
	Log      *log.Logger
}

// Sweep 执行一轮回收，返回删除数。单轮上限 MaxSweep（防 TTL 误配短引发雪崩）；
// prod 环境泳道跳过（灰度泳道回收策略留后续）；run 查询失败跳过该 lane（fail-open 下轮再试）。
func (g *LaneGC) Sweep(ctx context.Context) int {
	now := g.Now()
	ttl := g.TTL
	if ttl <= 0 {
		ttl = 72 * time.Hour
	}
	maxSweep := g.MaxSweep
	if maxSweep <= 0 {
		maxSweep = 20
	}
	wls, err := g.Repos.ListAll(ctx)
	if err != nil {
		g.logf("laneGC: list all: %v", err)
		return 0
	}
	deleted := 0
	for _, w := range wls {
		if deleted >= maxSweep {
			break
		}
		if w.LaneID == "" || w.LaneID == LaneDefault {
			continue // 基线不回收
		}
		if et, err := g.EnvType(ctx, w.EnvID); err == nil && et == "prod" {
			continue // 生产泳道不自动回收
		}
		idleSince := w.CreatedAt
		if last, err := g.Runs.LastActive(ctx, w.TenantID, w.AppID, w.LaneID); err != nil {
			continue // 查询失败跳过，下轮再试
		} else if last.After(idleSince) {
			idleSince = last
		}
		if now.Sub(idleSince) <= ttl {
			continue // 仍在 TTL 内
		}
		if err := g.Repos.Delete(ctx, w.ID); err != nil {
			g.logf("laneGC: delete %s: %v", w.ID, err)
			continue
		}
		g.logf("laneGC: 回收闲置泳道 workload=%s app=%s lane=%s（闲置 %s）",
			w.ID, w.AppID, w.LaneID, now.Sub(idleSince).Round(time.Minute))
		deleted++
	}
	return deleted
}

func (g *LaneGC) logf(format string, args ...any) {
	if g.Log != nil {
		g.Log.Printf(format, args...)
		return
	}
	log.Printf(format, args...)
}

// Start 启动周期回收（间隔 interval，<=0 用默认 30min）。返回停止函数。
// ctx 取消即退出（cmd/core 用 baseCtx 派生，进程退出即停）。
func (g *LaneGC) Start(ctx context.Context, interval time.Duration) (stop func()) {
	if interval <= 0 {
		interval = 30 * time.Minute
	}
	done := make(chan struct{})
	go func() {
		defer close(done)
		t := time.NewTicker(interval)
		defer t.Stop()
		for {
			select {
			case <-ctx.Done():
				return
			case <-t.C:
				g.Sweep(ctx)
			}
		}
	}()
	return func() { <-done }
}
```

`gcFakeRepo` 编译需补齐 Repository 其余方法——参照 `handler_test.go` 的 `stubRepo` 改为嵌入：`type gcFakeRepo struct { stubRepo; items []Workload; deleted []string }` 并覆写 ListAll/Delete（stubRepo 已有 ListAll）。注意 stubRepo 是 package 内既有类型，直接嵌入。

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/workload/ -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add internal/workload/lanegc.go internal/workload/lanegc_test.go
git commit -m "feat(workload): laneGC 闲置泳道 TTL 回收（TTL/上限/prod 护栏 + fail-open）"
```

---

### Task 3: cmd/core 装配 laneGC（env 配置 + bridge + 审计）

**Files:**
- Create: `cmd/core/lanegc_adapters.go`
- Modify: `cmd/core/main.go`（run() 内启动，位置在 stores 构建之后）

**Interfaces:**
- Consumes: Task 2 的 `LaneGC/LaneActivityChecker/EnvType`；`internal/devops/pipeline` 的 `ListRuns(ctx, appID, pipelineID, status string)`；`identityAuditAdapter`（既有）同款审计模式；`workload.Applier`（既有 wlRepo 已包装）。
- Produces: 进程内周期任务（env `PAAS_LANE_TTL` / `PAAS_LANE_GC_INTERVAL` 控制）。

- [ ] **Step 1: 写 bridge**（`cmd/core/lanegc_adapters.go`）

```go
package main

import (
	"context"
	"time"

	"github.com/aitoys/paas/internal/devops/pipeline"
	"github.com/aitoys/paas/internal/workload"
)

// laneRunChecker 桥接 pipeline.ListRuns -> workload.LaneActivityChecker（依赖倒置）。
// 最近活跃 = 该 (app, lane) 全部 run 的最近 FinishedAt（run.Branch == lane 是 L1 锁定的关联约定）。
type laneRunChecker struct{ runs pipeline.RunRepository }

func (c laneRunChecker) LastActive(ctx context.Context, tenantID, appID, lane string) (time.Time, error) {
	ctx = tenant.WithTenant(ctx, tenantID)
	runs, err := c.runs.ListRuns(ctx, appID, "", "")
	if err != nil {
		return time.Time{}, err
	}
	var last time.Time
	for _, r := range runs {
		if r.Branch != lane || r.FinishedAt.IsZero() {
			continue
		}
		if r.FinishedAt.After(last) {
			last = r.FinishedAt
		}
	}
	return last, nil
}
```

已确认：`pipeline.RunRepository.ListRuns(ctx, appID, pipelineID, status string)`、`PipelineRun.FinishedAt time.Time`（非指针，零值判断用 `IsZero()`）、`PipelineRun.Branch string`（run.Branch==lane 是 L1 锁定约定）。ListRuns 按 ctx tenant 过滤（WithTenant 必需），`tenant` 包为 `github.com/aitoys/paas/pkg/tenant`。

- [ ] **Step 2: main.go 装配**（run() 中 stores 就绪后）

```go
	// 泳道闲置回收（L3）：env PAAS_LANE_GC_INTERVAL=0 禁用；TTL 默认 72h。
	if v := envDuration("PAAS_LANE_GC_INTERVAL", 30*time.Minute); v > 0 {
		gc := &workload.LaneGC{
			Repos:   wlRepo, // Applier 装饰过的 repo（Delete 投影删 CRD）
			Runs:    laneRunChecker{runs: pipeRuns},
			EnvType: envStore.EnvType, // environment.Repository 既有方法（与 workload handler 同源注入）
			TTL:     envDuration("PAAS_LANE_TTL", 72*time.Hour),
			MaxSweep: 20,
		}
		stopGC := gc.Start(ctx, v)
		defer stopGC()
		logger.Printf("laneGC 启动：间隔 %s TTL %s", v, gc.TTL)
	}
```

按 main.go 实际变量名调整（wlRepo/pipeRuns/envStore 以现有装配代码为准；若 pipeline runs store 变量名不同用实际名）。`envDuration` 若无则写内联 helper（`time.ParseDuration(os.Getenv(...))` 容错默认值）。

- [ ] **Step 3: 构建验证**

Run: `go build ./cmd/core/ && go vet ./cmd/core/`
Expected: 编译通过

- [ ] **Step 4: 全量测试**

Run: `go test ./...`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add cmd/core/lanegc_adapters.go cmd/core/main.go
git commit -m "feat(core): laneGC 装配——env 可配间隔/TTL，baseCtx 生命周期启停"
```

---

### Task 4: 前端泳道矩阵（EnvironmentDetail）

**Files:**
- Modify: `frontend/console-user/src/views/EnvironmentDetail.vue`（应用部署矩阵 section）

**Interfaces:**
- Consumes: 既有 `/api/workloads`（响应含 `laneId`，L1 已落）。

- [ ] **Step 1: 矩阵升级**——应用部署矩阵从「应用 × 环境」改为「应用 × 泳道」分组：
  - 行 = 应用（该环境内有 Workload 的应用）。
  - 列 = 泳道集合（该环境全部 `laneId` 去重：`default` 固定第一列 + 各 feature 泳道按名排序）。
  - 每格：就绪比 `ready/replicas`（复用现有 ready tag 逻辑）+ 服务数。
  - feature 列表头加 warning 色 + 「泳道」前缀；某应用无该泳道 Workload 的格显灰字「↩ 基线」（表示该服务流量降级 default）。
  - 泳道列头加删除按钮（跳现有删除 Workload 流程即可，不新建删除 API）。

实现要点（`<script setup>` 内）：

```ts
// 泳道矩阵：按 (应用 × 泳道) 聚合本环境 Workload。
const laneCols = computed(() => {
  const lanes = new Set(workloads.value.map((w: any) => w.laneId || 'default'))
  lanes.add('default')
  return [...lanes].sort((a, b) => (a === 'default' ? -1 : b === 'default' ? 1 : a.localeCompare(b)))
})
const laneMatrix = computed(() => {
  // map[appId][lane] -> { ready, replicas, count }
  const m: Record<string, Record<string, { ready: number; replicas: number; count: number }>> = {}
  for (const w of workloads.value) {
    const lane = w.laneId || 'default'
    m[w.appId] ??= {}
    const cell = m[w.appId][lane] ??= { ready: 0, replicas: 0, count: 0 }
    cell.ready += w.ready || 0
    cell.replicas += w.replicas || 0
    cell.count++
  }
  return m
})
```

模板：`el-table`（行=应用，`v-for="lane in laneCols"` 动态 `el-table-column`，default 列 label「基线」、feature 列 label `泳道 ${lane}` + warning tag；空格渲染 `<span class="lane-fallback">↩ 基线</span>`）。

- [ ] **Step 2: 构建验证**

Run: `cd frontend && pnpm build`
Expected: vue-tsc + vite 通过

- [ ] **Step 3: Commit**

```bash
git add frontend/console-user/src/views/EnvironmentDetail.vue
git commit -m "feat(console-user): 环境详情泳道矩阵——应用×泳道就绪视图 + 降级基线角标"
```

---

### Task 5: trace 泳道着色（useSpanTree + 两视图）

**Files:**
- Modify: `frontend/console-user/src/composables/useSpanTree.ts`（加 helper）
- Modify: `frontend/console-user/src/views/Observability.vue`（trace 展开行 + 图例）
- Modify: `frontend/console-user/src/views/app-tabs/AppObservability.vue`（最近链路同款）

**Interfaces:**
- Consumes: `Span.tags`（`Record<string, string>`，后端已透传 `paas.lane` 属性进 tags）。

- [ ] **Step 1: useSpanTree 加 helper**

```ts
/** spanLane：取 span 的泳道（paas.lane 属性；无 = default 基线）。 */
export function spanLane(span: Span): string {
  return span.tags?.['paas.lane'] || ''
}
```

- [ ] **Step 2: 两视图着色**——span 行渲染处（瀑布图行）：

```html
<span v-if="spanLane(row.span)" class="lane-chip">🛣 {{ spanLane(row.span) }}</span>
```

```css
.lane-chip {
  display: inline-block; margin-left: 6px; padding: 0 6px;
  border-radius: 3px; font-size: 11px;
  background: var(--el-color-warning-light-9);
  color: var(--el-color-warning-dark-2);
}
```

trace 展开区顶部加图例（仅当该 trace 含 lane span 时显示）：「🛣 泳道标识——该 span 流量走了对应 feature 泳道；无标识 = default 基线」。两个视图同款（DRY：样式可放共用处或各自 scoped 复制，与现有两视图做法一致）。

- [ ] **Step 3: 构建验证**

Run: `cd frontend && pnpm build`
Expected: 通过

- [ ] **Step 4: Commit**

```bash
git add frontend/console-user/src/composables/useSpanTree.ts frontend/console-user/src/views/Observability.vue frontend/console-user/src/views/app-tabs/AppObservability.vue
git commit -m "feat(console-user): trace 泳道着色——paas.lane chip + 图例（运行态泳道视图）"
```

---

### Task 6: e2e 验证 + k8s 部署

**Files:** 无新文件（部署 + 验证）。

- [ ] **Step 1: 全量回归**

Run: `go test ./... -race` + `cd sdk/paas-registry && go test ./...` + `cd frontend && pnpm build`
Expected: 全绿

- [ ] **Step 2: 部署 k8s**

Run: `./scripts/deploy-k8s.sh`
Expected: 镜像推送 + helm upgrade + rollout 成功

- [ ] **Step 3: e2e 验证点**（curl + 界面）：
  1. `/livez` 200；core 日志出现 `laneGC 启动`。
  2. TTL 临时调短（`--set laneGC.interval=1m --set laneGC.ttl=2m` 或 env overlay）→ 建一个 feature 泳道 Workload → ~3min 后被自动删 + 日志 `回收闲置泳道`。
  3. paas-shop feature 泳道服务（不挂 middleware）调下游 → Jaeger trace 中下游 span `paas.lane=feature-x`。
  4. 环境详情页泳道矩阵渲染正确（feature 列 + 「↩ 基线」角标）。

- [ ] **Step 4: 更新 CLAUDE.md**（泳道 L3 章节：三件套落地 + env 配置 + 诚实边界），Commit：

```bash
git add CLAUDE.md docs/
git commit -m "docs: 泳道 L3 落地记录（零改动染色 + TTL 回收 + 泳道可视化）"
```
